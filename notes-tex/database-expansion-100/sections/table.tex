%% notes-tex/database-expansion-100/sections/table.tex
%% [[experiments.database.expansion-100]]
%% https://github.com/Mjvolk3/torchcell/tree/main/notes-tex/database-expansion-100/sections/table.tex
%% SOURCE: experiments/database/scripts/build_candidate_datasets_table.py

\section{The ranked list\secstatus{todo}}\label{sec:list}

\input{tables/candidates}

\clearpage
\input{tables/sources}

\clearpage
\section{What the list adds up to\secstatus{todo}}\label{sec:counts}

\input{tables/counts}

All 161 rows carry roughly $1.8\times10^8$ genotype-by-environment instances and
$5.7\times10^8$ individual measurements. Both are lower bounds: rows whose axes could not
be pinned down contribute nothing to either total, and the instance figures marked
$\dagger$ are products of the two axes rather than counts the papers report.

The totals are dominated by a handful of rows, so quoting them without naming those rows
would mislead. de Boer 2020 alone supplies $10^8$ instances, Turco 2023 $3.8\times10^7$ and
Lee 2014 $1.3\times10^7$. Of those three, Turco's contribution is not net new, since it is a
meta-aggregation of screens this table lists separately, and Lee's is not currently reachable
(Sec.~\ref{sec:status}). de Boer's stands, and it is a different kind of instance from the
rest: an 80-base promoter with an expression readout, not a strain with a phenotype.

The honest summary is that this list contains one dataset larger than everything already
built, one larger than everything else here, and about $2\times10^7$ instances spread across
the remaining 150 rows.

Wave 1 reads differently from the list as a whole, which is the point of banding it. Its
class mix is 18 expression and single-cell rows, 10 natural variation, 8 CRISPR library
screens and 6 metabolite rows, against a full-list mix in which tolerance screens are the
largest group. Its sequence bases shift the same way: 14 of the 50 are
\term{reference-only}, meaning the environment rather than a strain edit carries the
perturbation, which is what a condition-crossed expression layer looks like.

\clearpage
\section{Rank changes against the previous pass\secstatus{todo}}\label{sec:moves}

\input{tables/swaps}
