---
id: zsqveujt98kfosyaso7ws64
title: Expansion Bacteria
desc: ''
updated: 1790300398673
created: 1790300398673
---

The bacterial half of the dataset expansion: candidate *E. coli* and *P. putida* datasets
for the substrate, paired with `notes-tex/database-expansion-bacteria/` and generated by
`experiments/database/scripts/build_bacteria_candidate_datasets_table.py`. The yeast
sibling is [[experiments.database.expansion-100]].

## 2026.09.24 - Schema blockers, established by running the validators

Two things gate every bacterial row, and a third that was expected to gate them does not.
All three were checked by constructing the objects, not by reading the code, because the
question is what the code accepts.

Run under `PYTHONPATH=$WT` with the worktree interpreter:

```python
from torchcell.datamodels.schema import ReferenceGenome, DeletionPerturbation
ReferenceGenome(species="Escherichia coli", strain="MG1655")   # OK
ReferenceGenome(species="Pseudomonas putida", strain="KT2440") # OK
for name in ["b0002", "PP_0001", "ECK0002", "thrA", "YAL001C"]:
    DeletionPerturbation(systematic_gene_name=name, perturbed_gene_name=name)
```

| identifier | result |
|---|---|
| `b0002` (E. coli b-number) | REJECTED, "Invalid systematic gene name format" |
| `PP_0001` (P. putida locus tag) | REJECTED, same |
| `ECK0002` (EcoCyc) | REJECTED, same |
| `thrA` (gene symbol) | REJECTED, same |
| `YAL001C` (yeast) | ACCEPTED |

**1. `GenePerturbation.validate_sys_gene_name` is yeast-only.** It admits
`Y[A-P][LR]\d{3}[WC](-[A-Z])?`, `Q\d{4}` and `YNC[A-Q]\d{4}[WC]` and nothing else
(`torchcell/datamodels/schema.py:87-103`). This is load-bearing beyond naming, because
`Genotype` derives its content identity from the sorted systematic names of its
perturbations, so the namespace decides node identity in the graph. A b-number and a PP\_
locus tag have to stay distinguishable namespaces rather than being merged into one
string field. The fix is additive in the admission-gate sense: accepting a bacterial
namespace alongside the yeast patterns changes no record any served dataset holds, so no
served closure moves. Same shape as the segregant genotype added for Bloom 2019.

**2. The genomes tier has no bacterial assembly set.**
`torchcell/sequence/genome/registry.py` defines `SGD_S288C_R64` and `PETER2018_1011`,
both yeast, and `torchcell/sequence/genome/` holds only a `scerevisiae` subpackage. Each
host needs its reference release pinned by sha256 there before any row's sequence basis
resolves to bytes. Provisioning work, not a design question, but on the critical path.

**3. `ReferenceGenome` needs nothing.** `species` and `strain` are free strings with a
`ploidy` default of haploid, which is correct for both bacteria. The organism was never
hard-coded at the reference; it was hard-coded one level down, at the gene identifier.

## 2026.09.24 - The ranked list, and what the ordering rule costs

Generated by `experiments/database/scripts/build_bacteria_candidate_datasets_table.py`
into `notes-tex/database-expansion-bacteria/`. Rows were verified by four parallel
research agents, one per area, whose raw JSON is the session scratchpad input to the
converter; the committed script holds the curation as literals.

**75 candidates**, 50 recommended and 25 reserve. Ordering is measurements, meaning
instances times phenotype dimensionality, descending, with instances then tier as
tie-breaks. This is the requested scale-and-density rule and replaces the yeast list's
band-first ordering.

| | count |
|---|---|
| recommended fifty | 39 E. coli, 11 P. putida |
| tranche 1 (20) | 14 E. coli, 6 P. putida |
| per-host floor of 6 in tranche 1 | did NOT bind; measurement order holds throughout |
| mirror a built yeast dataset | 49 of 50 |
| figures sourced this pass | 48 of 50 |
| data location confirmed live | 29 of 50 |
| blocked on per-record data | 3 of 50 |
| corpus, not net new until split | 6 of 50 |

Top row is Fuhrer 2017 at 2.6e8 measurements: 3,807 Keio deletion mutants, over 34,000
MS injections, 7,534 m/z features. Then two barcoded transposon fitness datasets at
1.4e7 and 1.2e7.

### The cost of ranking on measurements

The rule pushes metabolic engineering into the reserve, and the size of the effect is
the main finding of the pass.

| row group | n | median measurements | median rank | in the fifty |
|---|---|---|---|---|
| production + combinatorial + tolerance | 24 | 145 | 62 | 3 |
| transposon fitness + chemical genomics | 16 | 282,906 | -- | 16 |

The ratio of medians is about 1,944. Twenty-one of the twenty-five reserve rows are
engineering rows. Neither figure is wrong: a titer on six strains is six numbers. What
the ordering encodes is that worth equals volume, which for inverse strain design is a
defensible default and an incomplete one. A portfolio rule needs no new data, only the
class field the records already carry.

### Corrections to the pasted candidate tables

The tables that seeded this pass came from a research session and carried errors. Each
below was checked against a fetched page.

- There is no Science 2018 genome-wide E. coli CRISPRi paper. That row is Wang et al.
  2018 Nature Communications, 55,671 guides plus 400 controls, not about 92,000. The
  92,000 figure belongs to the Bikard-lab libraries.
- Rousset 2018 screens essential genes and phage host factors, not antibiotics. The
  antibiotic-panel knockdown screen is a separate, later paper.
- The largest Babu double-mutant screen is PLoS Genetics 2014, not Nature, and only
  42,705 high-confidence pairs of more than 600,000 combinations were released.
- The two conjugation-based genetic-interaction screens are blocked: demonstration pairs
  and a linkage curve rather than genome-wide per-pair values.
- No author named Teng is on the P. putida biosensor paper; it is Menasalvas et al. 2025
  with Eng co-corresponding.
- The indigoidine knockdown row is 2 genotypes, not 14: the 14 guides sit in one
  multiplexed array in a single strain.
- The P. putida transposon library is Putida_ML5, 185,401 insertions and 152,810 in-gene
  over 5,213 of 5,661 genes. The "about 100,000 mutants, about 4,800 genes" figures
  conflate library size with fitness coverage.
- The Keio collection is BW25113, not MG1655. See the K-12 naming decision below.

### Decisions worth keeping

- **Sequence bases say K-12, not MG1655.** The deletion collection is BW25113 and the
  knockdown libraries are mostly MG1655, and those are different genomes. Naming one
  strain would be a sequence-level error in a substrate whose claim is sequence-level
  fidelity; K-12 records the lineage and defers the background to per-row provenance.
- **EcoCyc is excluded, not ranked.** Its 4,688 genes and 6,124 regulatory interactions
  are annotations of one genome, so under a measurement ranking a curation effort would
  outrank every real screen. It belongs in the reference layer.
- **One duplicate was merged.** Two agents returned putidaPRECISE321 under different
  keys; counting one transcriptome compendium twice would inflate both the row count and
  the measurement total.
- **The transposon rows overlap.** One released P. putida fitness matrix, 4,732 genes by
  332 samples and fully dense, is a superset of the other transposon rows, and the E.
  coli compendium is a superset of the method paper's experiments. Build order should
  de-duplicate rather than follow rank order here.
