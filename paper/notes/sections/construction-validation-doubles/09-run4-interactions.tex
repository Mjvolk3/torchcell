\section{Digenic interaction versus Costanzo --- measured, and not yet usable}
\label{sec:run4-eps}

The design supports the comparison cleanly. Both constituent singles of every double sit on
the same plate, so

\begin{equation}
  \varepsilon_{ab} \;=\; f_{ab} \;-\; f_a f_b
  \label{eq:eps}
\end{equation}

is formed entirely within a plate --- no cross-plate normalization enters the interaction
term, and the three plates give three independent replicates of the same quantity, which is
what the across-plate bootstrap turns into an error bar.

Run against the published Costanzo $\varepsilon$ for the same pairs, the answer is
\textbf{$r=-0.245$ ($p=0.44$, $n=12$)}: no agreement, and the sign is wrong.

\subsection{Every double, side by side}

The full comparison, one row per constructed double, sorted by the published
$\varepsilon$. The table is generated by
\texttt{run4\_doubles\_48h.py} straight from
\path{run4_doubles_vs_reference.csv} --- no number here is transcribed by hand.

\notetab{%
\small
\input{../../experiments/W019-echo-crispr-array/results/run4_interactions_table}
}{Every constructed double, sorted by published $\varepsilon$.
\textbf{Bold marks Costanzo's intermediate-confidence tier}, $P<0.05$ and
$|\varepsilon|>0.08$ --- applied identically to both $\varepsilon$ columns, so ours is bolded
from our $p$ and theirs from their published $p$, and the two are directly comparable.
That tier is quoted verbatim from the Costanzo 2016 SI in the torchcell library mirror
(\texttt{costanzoGlobalGeneticInteraction2016/si/si1.md}): ``\emph{We suggest three different
thresholds [lenient ($P<0.05$), intermediate ($P<0.05$ and $|\varepsilon|>0.08$), and
stringent confidence ($P<0.05$ and $\varepsilon>0.16$ or $\varepsilon<-0.12$)] that strike
different balances between false negatives and false positives}.'' The intermediate tier is
the one the paper itself works at --- every genetic-interaction figure in it is drawn at
$|\varepsilon|>0.08$, $P<0.05$ --- and recomputing it over the reference table's 44 scored
pairs reproduces the stored flag exactly.
Read the $f_a f_b$ column first: the multiplicative expectation exceeds 1.0 for ten of
thirteen pairs, which is what drives every $\varepsilon$ negative.
SE$_{\text{within}}$ is colony scatter propagated by the delta method; SE$_{\text{across}}$ is
the scatter of the three plate-level estimates; $p$ is a two-sided $t$-test on those three
($df=2$). Generated by \texttt{run4\_doubles\_48h.py}; no value transcribed by hand.
\textbf{Bold here means reproducible, not correct} --- see the caveat below.}

\notefig{\incsvg{W019-echo-crispr-array/run4/run4_interactions_forest}}%
  {The same comparison per double, as intervals rather than points. Our $\varepsilon$ carries
   its across-plate SE; the Costanzo value is marked on the same row. The scatter of
   Fig.~\ref{fig:epsscatter} answers whether the two agree overall; this answers, for each
   double, where our interval sits and where the published point falls relative to it.
   Rings mark the calls, using the same bar on both sides ($p<0.05$ and
   $|\varepsilon|>0.08$) --- and they never coincide.}

Read the $f_a f_b$ column first. It is the multiplicative expectation, and for ten of the
thirteen pairs it exceeds 1.0 --- which cannot be right for two deletion mutants and is what
drives every $\varepsilon$ in the fourth column negative. YPL046C$+$YPL081W has no published
$\varepsilon$ because it is the novel pair; it was constructed precisely because no reference
value exists.

\subsection{Our own significance --- and which error term you use decides everything}
\label{sec:sig}

Section~\ref{sec:whyfixable} argued that a big-but-insignificant interaction is fixable
because the error bar is set by how many colonies we plate, and that is our choice. This
round is the first test of that argument, and the answer is more interesting than expected:
\textbf{colony count was never the binding constraint.}

Two error terms can be put on $\varepsilon$, and the table reports both.

\notesec{Within-plate} $\varepsilon$ is a function of three strain means measured on the
same plate, so by the delta method
\begin{equation}
  \operatorname{var}(\varepsilon) \;=\;
  \operatorname{var}(f_{ab}) \;+\; f_b^{2}\operatorname{var}(f_a)
  \;+\; f_a^{2}\operatorname{var}(f_b),
\end{equation}
each variance being $(\text{colony SD}/\sqrt{n})^{2}$. This is the term Costanzo's own
$p$-value is built from, and the one that 14 colonies per plate rather than 4 was supposed to
shrink. It averages \textbf{0.058} across the thirteen doubles.

\notesec{Across-plate} The scatter of the three per-plate $\varepsilon$ values, which carries
the batch effect the within-plate term cannot see. It is \textbf{larger than the within-plate
term for 12 of 13 doubles}, by a median factor of \textbf{2.4} and up to \textbf{8.5}.

That gap is the finding. Judged on colony scatter alone, \textbf{11 of 13} doubles would
clear $p<0.05$ (median $|\varepsilon|/\mathrm{SE} = 5.9$). Judged honestly, on three
independent plate-level estimates, \textbf{2 of 13} do. Plating deeper does buy precision
within a plate --- it just buys precision on a quantity whose plate-to-plate reproducibility
is the actual limit. The lever for this assay is \emph{more plates}, not more colonies per
plate, which is the opposite of what Section~\ref{sec:whyfixable} assumed.

\notesec{Read the \texorpdfstring{$p$}{p} column with the caveat below} The test is a
two-sided $t$-test on three plate-level estimates, $df=2$ --- weak by construction, so a
non-significant call here is poor evidence of absence. And more importantly: with the WT
shift of Section~\ref{sec:wtshift} unresolved, this $p$ measures how \emph{reproducibly} we
recover a biased $\varepsilon$, not whether that $\varepsilon$ is right. Precision is not
accuracy. The two doubles that do clear $p<0.05$ (YER079W$+$YLR312C-B, YLL012W$+$YPL046C) are
\emph{not} the two Costanzo calls significant, and neither should be reported as a detected
interaction.

\subsection{Why this is an artifact and not a result}

\textbf{It should not be read as a failure to reproduce Costanzo's interactions.} Four
observations, all from \path{experiments/W019-echo-crispr-array/results/run4_interactions.csv},
identify it as a normalization problem upstream of Equation~\eqref{eq:eps}:

\notetab{%
\begin{tabular}{lr}
\toprule
diagnostic & value \\
\midrule
singles scoring \emph{above} the on-plate WT & \textbf{8 of 12} (up to 1.333) \\
multiplicative expectation $f_a f_b > 1$ & \textbf{10 of 13} pairs (up to 1.712) \\
sign of $\varepsilon$ & \textbf{negative for all 13} \\
$\mathrm{corr}(\varepsilon,\; f_a f_b)$ & $\mathbf{-0.926}$ ($p=5\times10^{-6}$) \\
\bottomrule
\end{tabular}
}{Four diagnostics identifying the interaction result as a normalization artifact upstream of Equation~\eqref{eq:eps} rather than a failure to reproduce Costanzo.}

A multiplicative null above 1.0 for two deletion mutants is not a meaningful expectation, and
an $\varepsilon$ that correlates at $-0.93$ with its own expectation term is tracking the
singles, not the doubles. Concretely, for the strongest published interaction in the panel:

\notetab{%
\begin{tabular}{lrrr}
\toprule
YDR057W+YGL087C & $f_a$ & $f_b$ & \\
\midrule
run 4 fitness & 1.243 & 1.163 & \\
expectation $f_a f_b$ & & & 1.446 \\
observed $f_{ab}$ & & & 0.788 \\
$\varepsilon$ (ours) & & & $\mathbf{-0.654}$ \\
$\varepsilon$ (Costanzo) & & & $+0.098$ \\
\bottomrule
\end{tabular}
}{The strongest published interaction in the panel, worked through. The $-0.654$ is arithmetic from two inflated singles, not a measured epistasis.}

That $-0.654$ is arithmetic from two inflated singles, not a measured epistasis.

\subsection{Where the inflation comes from}
\label{sec:wtshift}

The singles are on the same scale as run 3's --- just shifted. Comparing our two rounds
directly, the eleven strains common to both correlate at \textbf{$r=0.685$ ($p=0.020$)}, but
run 4 sits a near-uniform \textbf{$1.26\times$} above run 3 (median ratio; dividing it out
leaves a mean absolute difference of 0.111). In run 3 every single scored \emph{below} WT
(0.67--0.92); in run 4 most score above it.

\notesec{Two correlations, not one} Two similar numbers appear in this document and they are
\emph{different comparisons} on the same eleven strains --- worth separating, because read
together they look like one statistic quoted twice:

\notetab{%
\begin{tabular}{llrr}
\toprule
comparison & against & $r$ & $p$ \\
\midrule
run 4 singles vs run 3 singles & our own earlier round & 0.685 & 0.020 \\
run 4 singles vs Costanzo SMF & the published reference & 0.706 & 0.015 \\
\bottomrule
\end{tabular}
}{The two single-mutant correlations reported for run 4. Both are $n=11$ over the same
strains; they differ in what run 4 is compared \emph{to}. Neither is a restatement of the
other.}

This matters for $\varepsilon$ and not for the correlations because \textbf{$\varepsilon$ is
not scale-invariant}. Under a uniform inflation $f \to k f$,

\begin{equation}
  \varepsilon' \;=\; k f_{ab} - k^{2} f_a f_b,
\end{equation}

so the expectation term grows quadratically while the observed term grows linearly, driving
$\varepsilon$ negative in proportion to $f_a f_b$ --- exactly the $-0.926$ correlation
observed. A Pearson correlation, by contrast, is invariant to $k$, which is why the singles
still reproduce the published values at $r=0.706$ while every interaction is corrupted.

\notefig{\incsvg{W019-echo-crispr-array/run4/run4_interactions_vs_reference}}%
  {\label{fig:epsscatter}Run 4 $\varepsilon$ against published Costanzo $\varepsilon$, the two Costanzo-significant
   pairs marked. Every point sits below zero and the published spread is not recovered. This
   panel is published as the record of a diagnosed artifact, not as a measurement of
   epistasis.}

\subsection{What is not yet known}

\textbf{Why the WT reference moved is untested.} At the colony level the WT median is
\emph{higher} than the mutant median on all three plates (ratio 0.90--0.94), so this is not
simply sick or contaminated WT wells. Run 3 named its wild type \texttt{BY4741} explicitly;
run 4's strain list gives it as \verb|'-|. Whether that is a different stock, a different
plating density, or something else has not been checked, and no correction should be applied
before it is --- rescaling to make the numbers look right would bury the question.

\subsection{Diagnostic: it is not the wild-type wells}
\label{sec:wtdiag}

\textbf{This subsection reports a diagnostic, not a correction.} Re-scoring against the plate
median rather than the WT wells does not make the numbers right --- it makes them differently
normalized, and the plate median is a poor stand-in for a wild type here because 25 of the 26
strains are mutants and 13 of those are doubles that genuinely grow less, so it compresses the
very range the assay exists to measure. It was run to answer one narrow question: is the shift
specific to the WT \emph{wells}, or a property of the whole round?
(\path{experiments/W019-echo-crispr-array/scripts/run4_wt_reference_diagnostic.py}.)

\notetab{%
\begin{tabular}{lrr}
\toprule
 & WT wells (as shipped) & plate median \\
\midrule
singles above the reference & 8/12 & 9/12 \\
median run 4 / run 3 ratio & 1.263 & 1.368 \\
$f_a f_b > 1$ & 10/13 (max 1.712) & 10/13 (max 1.989) \\
$\varepsilon$ negative & 13/13 & 13/13 \\
$\mathrm{corr}(\varepsilon, f_a f_b)$ & $-0.926$ & $-0.939$ \\
vs Costanzo SMF & $r=0.706$ & $r=0.708$ \\
\bottomrule
\end{tabular}
}{Swapping the normalization denominator. Every diagnostic is unchanged or slightly worse
under the plate median, which rules the WT wells out as the explanation.}

\textbf{The answer is no.} Every diagnostic is unchanged or marginally worse under the plate
median. The inflation is not an artifact of the wild-type wells, so the hypothesis in the
previous subsection --- a different WT stock or plating density --- is not supported and
should not be pursued as though it were.

What the swap does clarify is where the asymmetry actually sits. It is not that the reference
is low; it is that \textbf{the doubles are low relative to the singles}, and no choice of
denominator changes that, because a common factor cancels from the ratio $f_{ab}/(f_a f_b)$
only when it applies to all three equally. Here the singles sit near 1.2 and the doubles near
0.8 against either reference.

\subsection{Replicate structure --- ours is technical, theirs is not}
\label{sec:replicate-structure}

Two facts about how these numbers are made, which together explain most of the gap in
precision and bound what our error bars can mean.

\notesec{Every strain is plated from a SINGLE picked colony} All 14 wells of a strain on a
plate, and all three plates, descend from one picked colony grown up and diluted. That makes
the 42 colonies per strain \textbf{technical replicates of one picking event}, not independent
biological samples. It is what makes the experiment tractable, but it has a consequence that
must not be glossed: \textbf{the across-plate SE used throughout this section does not contain
colony-picking variance.} Re-randomizing position across three plates averages out position
and plate handling; it cannot average out a draw that was made once, upstream of all three.
So the honest error bar of Section~\ref{sec:sig} is still an \emph{under}-estimate with
respect to the biological unit, and the $p$ values inherit that.

\notesec{Costanzo's single-mutant fitness is averaged over hundreds of screens} From the SI
(\texttt{costanzoGlobalGeneticInteraction2016/si/si1.md}, ``Single mutant fitness standard''):
``\emph{Colony size measurements of SGA deletion and TS array mutant strains were based on an
average of 350 replicate control screens \ldots\ query mutant strains were based on an average
of 17 replicate control screens}.'' Their SMF is not a 4-colony number --- the 4 colonies
apply to the double-mutant screens. Against 350 (or 17) independent control screens, three
plates from one colony is a different order of replication, and that alone is enough to expect
our agreement to be looser than their internal reproducibility.

\notesec{Not verified} A reported reproducibility of roughly $0.9$ between duplicate fitness
measurements is plausible from this design but \textbf{could not be confirmed from our
mirror}. The SI text describes fig. S2 as the reproducibility analysis and frames it in terms
of precision/recall of \emph{genetic interactions} across five replicate screens, not a
fitness scatter; no correlation coefficient for duplicate fitness appears in the extracted
text of either the paper or the SI. It may well sit inside a figure panel, which our OCR does
not read. Treat the $0.9$ as unconfirmed here rather than as absent.

\subsection{A single-colony wild type, and what it can and cannot explain}
\label{sec:weakwt}

The wild type is picked once per round like every other strain
(Section~\ref{sec:replicate-structure}), so the entire round's denominator rests on one
colony. If that colony happened to be a weak grower, everything normalized to it is inflated
by a constant --- and because the three plates share it, no amount of within-round replication
would reveal the problem. This is a much better candidate than the WT-wells hypothesis already
rejected in Section~\ref{sec:wtdiag}, and it fits two observations directly: singles landing
above 1.0, and run 4 sitting a near-uniform $1.26\times$ above run 3, since the two rounds
drew independent wild-type colonies.

\textbf{But it cannot be the whole story, and the arithmetic says so.} A reference error is a
common factor $k$ applied to every strain on the plate, so it cancels out of any ratio between
two strains:

\notetab{%
\begin{tabular}{lrrr}
\toprule
reference scale $k$ & mean single & mean double & double/single \\
\midrule
1.00 (as measured) & 1.066 & 0.809 & 0.758 \\
1.26 & 1.344 & 1.019 & 0.758 \\
1.50 & 1.600 & 1.213 & 0.758 \\
\bottomrule
\end{tabular}
}{A weak wild type rescales singles and doubles together, so the double-to-single ratio is
invariant to it. That ratio is 0.758 where a multiplicative model predicts roughly the mean
single itself ($\sim$1.07); the discrepancy therefore survives any choice of reference.}

So there are \textbf{two separable effects}, and conflating them is what made the earlier
sections chase one explanation:

\begin{enumerate}
  \item a \emph{per-round scale} on the reference --- consistent with a single-colony wild
        type, explains the $1.26\times$ between rounds and the singles above 1.0, and does not
        touch $\varepsilon$'s \emph{ordering};
  \item a \emph{doubles-versus-singles} discrepancy --- the double/single ratio is 0.758 where
        a multiplicative model wants $\sim$1.07 --- which no reference choice can produce and
        which is what actually corrupts $\varepsilon$.
\end{enumerate}

Effect (1) now has a plausible mechanism. Effect (2) does not, and it is the one that matters
for interactions.

\subsection{What is not yet known}

The WT-wells explanation is tested and rejected (Section~\ref{sec:wtdiag}); a single-colony
wild type plausibly accounts for the per-round scale but provably not for the
doubles-versus-singles gap (Section~\ref{sec:weakwt}). What remains open is that second
effect: why the doubles sit low relative to the singles under any normalization. It may be
partly real biology at 48\,h 12\,min on this medium; nothing here tests that.

What this round \emph{cannot} distinguish is a per-strain construction or picking effect from
a genetic one, because each strain enters the experiment through a single picked colony
(Section~\ref{sec:replicate-structure}). A double whose founding colony happened to be a poor
grower is indistinguishable, in these data, from a double with a real negative interaction ---
and with all three plates sharing that colony, no amount of within-round replication separates
them. \textbf{The discriminating experiment is independent pickings}: two or more colonies per
strain, carried as separate lineages through the whole round. That converts the replicate unit
from technical to biological and is the same change that would let the across-plate SE of
Section~\ref{sec:sig} mean what it currently only appears to mean.

Until then, no $\varepsilon$ from this round should be reported as an interaction, and no
rescaling should be applied to make the numbers look reasonable: every correction available
here is cosmetic and would bury the question.
