---
id: ogvbzxpeltnot5eyaykjuzv
title: '37'
desc: ''
updated: 1788913645350
created: 1788913645350
---

## 2026.09.08

- [x] 010 positive-panel report reviewed and corrected: the 0.877 calibration slope had no generating script and reproduces exactly on the whole test split while the nominated tail runs at 0.24; "two thirds real calls" was magnitude-only (2 of 18 under the Kuzmin call); rescue ordering is 82 percent published rungs; three 20-strain designs tie on the median objective ([[experiments.010-kuzmin-tmi.scripts.calibration_on_test_split]], [[experiments.010-kuzmin-tmi.scripts.inference_4_panel_rescue_decomposition]])
- [x] Query-pair-disjoint arm had never run: job 1599 died on `plot_edge_recovery_every_n_epochs: 0` (fixed b0b05419a). Launched the matched pair 1607 (random) and 1608 (disjoint), which differ only in the split file. Not the Table 10 row of the additive-baselines report: different partition (331/43/46 vs 285/67/68 query pairs), single split vs five-fold, mask arm vs KL
- [x] Draft figure "not what you learn, how you learn": the graph penalty never reaches the prediction (inference swap 1e-6) and decides whether training happens (lambda = 0 fails; hard mask, the lambda to infinity limit, reaches 0.31 at epoch 1 then predicts the mean; soft KL 0.446). Candidate for SI `fig:graphreg-empirical` [[experiments.025-solid-growth.scripts.graph_regularization_how_not_what]]
- [x] Mock-up `notes/assets/drawio/FigS-graph-regularization-sweep.drawio` (+ `.drawio.png`, generator `.gen.py`): the λ sweep with no-penalty and hard-mask as the extremes (panel b), gradient budget at probe epochs (c), discovery defined on graphs already in hand (d: regulatory head vs TFLink-only edges), overruling defined with Costanzo digenic ε (e), degree-matched random-graph control (f), and the Delta design strip (24 runs, ~2,016 GPU-h). Headless export recipe recorded in memory `drawio-headless-export-gilahyper`
- [x] Delta package for the graph-regularization sweep on the 010 build (the 025 full build is 3.2 TB; the 010 build is 1.5 GB with bit-identical labels): configs `cgt_010b_r_kl_005` / `r_mask_006` / `q_kl_007`, 010-native index artifacts, `delta_cgt.slurm`, preflight, sweep submitter, and the GilaHyper to Delta sync ([[experiments.025-solid-growth.scripts.delta_cgt]], [[experiments.025-solid-growth.scripts.make_010build_index_artifacts]])

## 2026.09.09

- [x] The four panels of the 010 additive-baselines report brought to the repo figure standard (panel letters at the outer top-left, framed legends, palette-order series, palette density ramp with a shared colorbar) and the PDF rebuilt; `panel_label` and the framed-legend rcParams added to `torchcell.utils` ([[experiments.010-kuzmin-tmi.scripts.additive_baseline_gene_interaction]], [[experiments.010-kuzmin-tmi.scripts.paired_prediction_agreement]], [[experiments.010-kuzmin-tmi.scripts.inference_run_consistency]], [[experiments.010-kuzmin-tmi.scripts.query_pair_disjoint_cv]])
- [x] Graph penalty against the point loss over training, from the four runs' W&B histories: 0.9999 of the objective for M01 to M03, 0.32 for the 025 replication (job 1598) whose edge normalization was already fixed, and 1598 still reaches the 010 band, so the dominance was not what produced the accuracy [[experiments.010-kuzmin-tmi.scripts.graph_penalty_vs_loss]]
- [x] Label z-score fit population named in the 025 trainer (`transforms.fit_on: subset | train`); the query-pair-disjoint KL arm queued for 23:00 (job 1609) fits on its training split, the constants for every population written to a result file, and the additive-baselines report's normalization row rewritten as what it is: Pearson-invariant hygiene, not a source of margin [[experiments.025-solid-growth.scripts.label_normalization_constants]], [[experiments.025-solid-growth.scripts.equivariant_cell_graph_transformer]]
- [x] The six additive nulls refit on the 025 build under both transformer arms: arm R reproduces the 010 numbers to 3e-13 for every closed-form model (the 025 labels and pinned split are the 010 ones), and arm Q, the exact query-pair-disjoint partition job 1609 will report on, puts the additive ridge at 0.185 and the nonlinear MLP at 0.141, which is what the transformer has to beat tonight [[experiments.025-solid-growth.scripts.additive_baselines_025]]
- [x] Additive-baselines report: section 3.3 rewritten to the facts (the graphs enter once, through the layer-1 penalty; not in the prediction, in the weights), B2 given its closed form and the per-screen offset it learns, B3 and B4 split out with equations that show they coincide on the random split, the capacity table reordered with a fit-by column, and the index-defect symptom tables and figure moved to the companion document with a pointer left behind
- [x] Delta package for the graph-regularization sweep on the 010 build (the 025 full build is 3.2 TB; the 010 build is 1.5 GB with bit-identical labels): configs `cgt_010b_r_kl_005` / `r_mask_006` / `q_kl_007`, 010-native index artifacts, `delta_cgt.slurm`, preflight, sweep submitter, and the GilaHyper to Delta sync ([[experiments.025-solid-growth.scripts.delta_cgt]], [[experiments.025-solid-growth.scripts.make_010build_index_artifacts]])
- [x] 010 positive-panel report landed and published to Zotero for review (`torchcell/notes-tex/010-positive-panel`, version `2026-09-09-19-00-02_2ba91614`). Sections 10 to 12 are the new material: why regulation acting on metabolism, the `inference_4` ranking, and the 20-strain pyruvate-node panel with its measured-overlap diagnostic ([[experiments.010-kuzmin-tmi.scripts.inference_4_panel_design]], [[experiments.010-kuzmin-tmi.scripts.inference_4_panel_score]], [[experiments.010-kuzmin-tmi.scripts.inference_4_panel_overlap]], [[experiments.010-kuzmin-tmi.scripts.inference_4_measured_diagnostic]])
- [x] Every number in the new sections re-checked against its result file: panel scores, the stratum table, the rescue decomposition (82.4 percent measured share), the engineering axis subsystems, and the Marquez-Zavala quotes against the mirror at sha256 `952805b4`. All reproduce
- [x] Section cross-references converted to `\label` / `\ref`. Inserting sections 10 and 12 had shifted the numbering, so the abstract pointed at Section 13 for a panel that renders as Section 12, and three more references were off by the same amount
- [x] Additive-baselines review: both ladder captions now say which bars carry an error bar and why (only B5, over its three seeds; the closed forms are single deterministic fits), the 025 ladder is cross-referenced as the disjoint counterpart of the 010 one, and the arm R against 010 parity scatter is cut in favor of the record-level dataset check that already proves the point [[experiments.025-solid-growth.scripts.additive_baselines_025]]
- [ ] Decide: cancel job 1608 (hard mask on the disjoint split; the mask arm already collapses on the random split, so 1608 cannot answer the split question) and queue `cgt_s0_q_kl_004` (soft KL on the disjoint split) in its place
- [ ] Delta canary before the sweep: one KL run to measure minutes per epoch with zero dataloader workers (GH RTX 6000 Ada: 19.4 min/epoch KL, 14 min mask; IGB: 58 min/epoch KL, 18 min λ = 0)
- [x] Delta disjoint arm was reading 010's own query-pair-disjoint split (285/67/68 pairs, B1 0.174) instead of arm Q's (331/43/46, B1 0.185), so its result would have compared to neither the arm Q nulls nor the GilaHyper disjoint run; the artifact builder now carries arm Q's partition into 010-build index space by query pair name and asserts the fold sizes reproduce exactly, and `gh_cgt.slurm` no longer falls back to the random-split config when an argument is dropped [[experiments.025-solid-growth.scripts.make_010build_index_artifacts]]

## 2026.09.10

- [x] Job 1640 ran the arm Q disjoint split overnight and hit the 12 h wall clock at epoch 36; on validation its best epoch reaches 0.199 against the additive ridge's 0.150, but it falls back through the ridge by epoch 17 and ends at 0.131, so the margin appears and is lost rather than holding as arm R's does [[experiments.025-solid-growth.scripts.additive_baselines_025]]
- [x] Additive-baselines report gained a per-epoch validation figure for both arms and filled the arm Q transformer bar in the ladder; both transformer bars are hatched because neither run has a test evaluation [[experiments.025-solid-growth.scripts.additive_baselines_025]]
- [ ] Score the surviving epoch 7 checkpoint of job 1640 on the arm Q test split, which is what turns the disjoint comparison against B1 0.185 and B5 0.141 into a real one
- [x] Carotenoid-precursor proximity of GEA1, VPS1, PCT1 and HXK2 in the nine graphs the 010 model trained on, as the graph-side counterpart to the FSEOF scan that could not separate them. HXK2 is the only one enriched: seven of the ten mevalonate-to-GGPP genes are its neighbors in the union graph at 6.9-fold, p = 1.0e-5, carried by STRING coexpression and a fusion edge to both HMG-CoA reductase paralogs. PCT1 reaches p = 2.3e-2 and does not survive the 120-test Bonferroni threshold, and GEA1 and VPS1 have zero backbone neighbors in every graph [[experiments.010-kuzmin-tmi.scripts.carotenoid_graph_enrichment]]
- [x] Yeast9 FBA baseline rerun on the trigenic screens' own medium (SD/MSG -His/Arg/Lys/Ura, from the ontology's `SGA_TM_SELECTION` through the new `SGA_TM_SELECTION_FBA` recipe; MSG bounded at the 0.165 supplement rate because opening it makes glutamate the carbon source), with the model default and an ammonium glucose-3.3 control (job 1671, 37 min, all 2.96 M solves optimal). Growth moves (94.3% of triples at wild type, 3.5% no growth) but the interaction correlation stays at zero (r = -0.0007); the frozen run's 231 nonzero tau were GLPK failures at glucose 1 that do not recur at 3.3. Panel g of `FigS-yeast9-fba` and `tab-fba-screen-medium` filled, the "rerun required" placeholder gone ([[experiments.007-kuzmin-tm.scripts.fba_screen_medium]], [[experiments.007-kuzmin-tm.scripts.fba_screen_medium_si]], [[torchcell.metabolism.media]])
- [x] DCell training step profiled on a gilahyper GPU at the cluster's batch of 600 in bf16-mixed, with the epoch-142 checkpoint copied from IGB (1.6 GB, plus the seven slurm logs) loaded strictly as the architecture check: 30.6 s per step, kernels busy 10%, 1.73 M kernel launches, the per-term gene-state gather 92% of host time and 88% of kernel time. Panel e of `FigS-dcell-training` replaces the CPU stand-in; the caption and the cost paragraph rewritten ([[experiments.006-kuzmin-tmi.scripts.dcell_training_gpu_profile]])

## 2026.09.11

- [x] `FigS-dcell-training` panel d in order: stages 1 to 7 (the cumulative chain, then the two `torch.compile` variants), the later-day reruns of stage 5 no longer drawn as a dashed range; caption updated, no number changed ([[experiments.006-kuzmin-tmi.scripts.dcell_training_wandb]])
