# notes-tex/eqtl-data-model/Makefile
# [[torchcell.datamodels.eqtl-data-model]]
# https://github.com/Mjvolk3/torchcell/tree/main/notes-tex/eqtl-data-model/Makefile
#
# references.bib is the union of the group and personal eqtl-data-model collections
# (the standard two-tier arrangement; see Makefile.common). The committed file was
# emitted by zotero_add_ref.py --emit-bibtex with keys pinned in Zotero (keyword
# zotero-pending marks the entries); `make bib` on a machine running Zotero +
# Better BibTeX regenerates it from the collections and drops the marker.
#
# The figure is exported from notes/assets/drawio/ by `make figures`.

PYTHON ?= /home/michaelvolk/miniconda3/envs/torchcell/bin/python
DRAWIO ?= /tmp/drawio-exec.AppImage

ZOTERO_COLLECTION          := VNDH4NMX
ZOTERO_PERSONAL_COLLECTION := 4VNJWJAW

include ../common/Makefile.common
