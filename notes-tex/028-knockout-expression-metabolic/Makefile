# notes-tex/028-knockout-expression-metabolic/Makefile
# [[experiments.028-knockout-expression.metabolic-phenotype-correlation]]
# https://github.com/Mjvolk3/torchcell/tree/main/notes-tex/028-knockout-expression-metabolic/Makefile

DOC := 028-knockout-expression-metabolic

# No Zotero collection: every number is our own computation on datasets already in
# the library, and each dataset is named inline with its DOI. `make bib` is not
# available; `make check` still runs and fails only on \cite keys, of which there
# are none.
ZOTERO_COLLECTION          :=
ZOTERO_PERSONAL_COLLECTION :=

# The figure is written by a script under experiments/028-knockout-expression/ to the
# 028 image directory under a STABLE name (no timestamp), the target of `make plots`.
PLOT_SLUG := 028-knockout-expression

PYTHON ?= /home/michaelvolk/miniconda3/envs/torchcell/bin/python

include ../common/Makefile.common
