# experiments/025-solid-growth/conf/cgt_s0_r_kl_fit_009.yaml
#
# ARM S0/R/KL + FITNESS, LOW WEIGHT. cgt_s0_r_kl_fit_008 with the fitness term at 0.1
# instead of 1.0, so the pair brackets whether the auxiliary helps, hurts, or is inert as
# its weight moves an order of magnitude. Same seed as _008 and as the control
# (cgt_s0_r_kl_000 on the same node), so the three differ in the fitness weight only.
#
#   sbatch -J 025-kl-fit0.1 experiments/025-solid-growth/scripts/igb_mmli_cgt.slurm cgt_s0_r_kl_fit_009
defaults:
  - cgt_s0_r_kl_fit_008
  - _self_

wandb:
  tags: [s0, split_R, graph_reg_kl, fitness_joint, igb_mmli, fitness_lambda_0.1]

regression_task:
  fitness_lambda: 0.1
