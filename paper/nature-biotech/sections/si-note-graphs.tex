%% ---- Supplementary Note [note:graphs]: the gene--gene graphs used as attention priors ----
%% Every number here is emitted by experiments/010-kuzmin-tmi/scripts/graph_statistics.py (also
%% writes tab-graphs.tex + tab-string-versions.tex); the figures are composed by
%% experiments/010-kuzmin-tmi/scripts/compose_graph_si_figures.py. Rerun those, not this file.
%% MISSING BIB KEYS (curation decision; not added here): SGD, TFLink, STRING. See REF-TO-ADD below.
\clearpage
\suppnote{note:graphs}{gene--gene graphs used as attention priors\secstatus{todo}}

Nine gene--gene graphs regularize the cell encoder's attention in the trigenic experiment, one
head per graph, and each enters the model only through the soft prior $\Omega_k$ of
Eq.~\ref{eq:graphreg}: none is used for message passing and none is imposed as a hard mask.
\supptab{tab:databases} and \suppfig{fig:graph-priors} report their size, overlap, and relation
to the other components of the cell representation; \suppfig{fig:graph-priors-2} their hubs,
the agreement of the two transcription-factor graphs, the drift of the STRING channels across
releases (\supptab{tab:string-versions}), and their neighborhood structure.

%% REF-TO-ADD: cherrySGDSaccharomycesGenome1998 (SGD)
%% REF-TO-ADD: liskaTFLinkIntegratedGateway2022 (TFLink)
%% REF-TO-ADD: STRING v12.0 (Szklarczyk et al. 2023; no key in notes/assets/bib/bib.bib yet)
\notesec{Sources} Two graphs are curated from the \emph{Saccharomyces} Genome Database (SGD;
Cherry et al.\ 1998): \emph{physical}, the protein--protein interactions SGD records per gene,
and \emph{regulatory}, SGD's regulation annotations as directed regulator$\to$target edges.
\emph{TFLink} (v1.0; Liska et al.\ 2022) is a directed transcription-factor--target compendium.
The remaining six are the evidence channels STRING v12.0 (Szklarczyk et al.\ 2023) scores
separately for \emph{S.\ cerevisiae}: neighborhood, fusion, co-occurrence, co-expression,
experimental, and database; an edge is any gene pair with a nonzero channel score, the cut
DANGO trains on. Every graph is restricted to the 6,607-gene S288C reference.

\notesec{Size, overlap, and structure} The graphs differ by a factor of three in the genes they
reach (2,191 to 6,474, leaving 133 to 4,416 genes of the reference without an edge) and by two
orders of magnitude in edges (11,085 to 996,199) (\suppfig{fig:graph-priors}a,\,b;
\supptab{tab:databases}). They are complementary rather than redundant: the Jaccard index of
edge sets exceeds 0.12 for a single pair, co-expression against experimental (0.44), and 52\%
of the 1,515,940 distinct pairs appear in exactly one graph, none in more than seven
(\suppfig{fig:graph-priors}c,\,e). Containment is asymmetric (\suppfig{fig:graph-priors}d):
76\% of physical and 82\% of neighborhood edges lie inside a dense STRING channel, and 43\% of
regulatory edges lie inside TFLink whereas 8\% of TFLink edges are regulatory. Since the prior
pulls a gene's attention toward its neighbors, what a graph exposes to the model is its
neighborhoods (\suppfig{fig:graph-priors-2}e): eight graphs are essentially one connected
component, co-occurrence splits into 486 near-clique components, and the mean number of genes
within two edges of a gene spans three orders of magnitude, from 5,038 (TFLink) to 22
(co-occurrence).

\notesec{Other components} \suppfig{fig:graph-priors}f relates each graph to the metabolic
network, the gene ontology, the essentiality labels, and the trigenic training genes of the same
cell representation. The three genome-context STRING channels and the database channel are
enriched for Yeast9 metabolic genes (26\% to 39\% of their covered genes, against 18\% of the
reference); the other five stay within five points of the reference, and the share of essential
genes stays within 16\% to 24\% for every graph. The five graphs that reach over 5,000 genes
cover 96\% to 100\% of the 1,400 Kuzmin 2018 trigenic genes and 82\% to 99\% of the 4,308
Kuzmin 2020 genes; database covers 77\% and 65\%, and neighborhood, fusion, and co-occurrence
34\% to 53\%. Against a degree-preserving random graph, physical, database, and co-occurrence
edges share a Yeast9 reaction 24 to 150 times more often than chance (0.3\%, 0.9\%, and 4.2\%
of edges), and every STRING channel and the physical graph share a GO biological process 2.7 to
7.5 times more often (15\% to 67\% of edges). The two transcription-factor graphs are the
exception on every pair-level measure: their regulator--target pairs share a reaction, a
subsystem, or a process no more often than the random reference (6\% to 7\% of edges share a
process, against 10\% at random).

\notesec{Hubs and the two transcription-factor graphs} The highest-degree gene differs in every
graph, and of the 74 genes in the nine top-ten lists only 12 recur
(\suppfig{fig:graph-priors-2}b). Regulatory and TFLink share 200 of their 628 regulators and
4,963 of 6,657 targets but only 16,817 of 223,620 directed edges (7.5\%), and the median
Jaccard index between a shared regulator's two target sets is 0.03
(\suppfig{fig:graph-priors-2}c); they are kept as separate priors.

\notesec{STRING releases} DANGO was built on STRING v9.1 and set its per-channel weights from
the change to v11.0; the CGT experiment uses v12.0. Every channel grows at each release and
loses part of what it had (\suppfig{fig:graph-priors-2}d; \supptab{tab:string-versions}): a
channel retains 42\% to 72\% of its pairs from v9.1 to v11.0 and 51\% to 75\% from v11.0 to
v12.0, and only 17\% (fusion) to 45\% (neighborhood) of a v12.0 channel's pairs were already
in v11.0. Whether that difference moves DANGO is tested in \suppnoteref{note:dango-repro}.

%% Figure source: notes/assets/drawio/FigS-graph-attention-priors.drawio (compose script above)
%%   -> figures/FigS-graph-attention-priors.pdf (auto-exported by `make fig`/`make paper`).
\begin{figure*}[t]
\tcfig{figures/FigS-graph-attention-priors.pdf}{\textbf{Gene--gene graphs used as attention
priors in the trigenic experiment.} Directed graphs (regulatory, TFLink) are compared as
undirected gene pairs; matrix axes in (c,\,d) drop the STRING prefix. (a)~Genes with at least
one edge, the hatched remainder to the 6,607-gene reference (dotted line) being the uncovered
genes, counted at right; distinct gene pairs; and the fraction of each graph's pairs found in no
other graph. (b)~Complementary cumulative degree distribution: fusion and co-occurrence have no
gene above degree 100, the two dense STRING channels fall off past degree $10^{3}$, and TFLink
carries the heaviest tail. (c)~Jaccard index of edge sets. (d)~Containment: the fraction of the
row graph's pairs present in the column graph. (e)~Distinct gene pairs supported by exactly $n$
graphs (52\% in one, 37\% in two, none in more than seven); the nine edge sets sum to
2,437,196 pairs, 1,515,940 distinct, 6.9\% of the 21.8 million pairs the vocabulary allows.
(f)~Relation to the other components of the cell representation: share of covered genes that
are Yeast9 (yeast-GEM 9.0.2) metabolic genes, that is, genes of a gene--reaction rule (1,161),
or essential, an SGD null-mutant \emph{inviable} phenotype in S288C (1,140), the dotted line
being the same share over the 6,607-gene reference; share of the Kuzmin 2018 (solid) and 2020
(hatched) trigenic gene panels the graph covers; and, on logarithmic axes, share of the graph's
gene pairs whose genes share a Yeast9 reaction, a Yeast9 subsystem (107 subsystems), or a GO
biological-process term (direct SGD annotations, root excluded, at most 500 genes per term;
1,944 terms), the open circle being the same share in a degree-preserving random graph
(configuration model, seed 0). Source:
\texttt{experiments/010-kuzmin-tmi/scripts/graph\_statistics.py}.}{fig:graph-priors}
\end{figure*}

%% Figure source: notes/assets/drawio/FigS-graph-attention-priors-2.drawio (same composer)
%%   -> figures/FigS-graph-attention-priors-2.pdf (auto-exported by `make fig`/`make paper`).
\begin{figure*}[t]
\tcfig{figures/FigS-graph-attention-priors-2.pdf}{\textbf{Shared pairs, hubs,
transcription-factor graphs, STRING releases, and neighborhood structure.} Matrix axes in
(a,\,b) drop the STRING prefix. (a)~Gene pairs shared by each pair of graphs (diagonal, the
graph's own count; logarithmic color): co-expression and experimental share 556,106 pairs,
physical and experimental 104,771, neighborhood and co-expression 119,758, the two
transcription-factor graphs 16,885. (b)~Genes among the ten highest-degree genes of at least
two graphs, colored by degree percentile in each graph (dash, absent from the graph); at right,
the opening words of the gene's SGD description. Top-degree gene per graph: DHH1 in physical
(3,606 partners), FKH1 in regulatory (2,991), GCN4 in TFLink (4,908), DUR12 in neighborhood
(936), CDC28 in fusion (84), SPS1 and KSP1 tied in co-occurrence (97), SSA1 in co-expression
(1,770), NAB2 in experimental (2,696), HFD1 in database (374); at the top 1\% (66 genes per
graph), 353 genes are hubs of one graph, 79 of two, 22 of three, 3 of four, and CDC28 of five.
(c)~Top: regulators, targets, and directed regulator$\to$target edges of the SGD regulatory
graph (511 regulators, 39,636 records) and TFLink (317 regulators), split into one-graph-only
(22,819 regulatory, 183,984 TFLink edges) and both. Bottom: for the 200 shared regulators, the
Jaccard index between the regulator's two target sets; the best case is FKH1 at 0.53 (1,747
shared targets). (d)~Per STRING channel, gene pairs per release in time order: pale bar the
release, solid part the pairs already in the previous release, hatched bar below zero the pairs
the previous release lost; the channels grow 2.2$\times$ (database) to 8.7$\times$ (fusion)
over the three releases. (e)~Per graph: fraction of covered genes in the largest connected
component, mean clustering coefficient, degree assortativity, and mean number of genes within
two edges of a gene (dotted line, the 6,607-gene reference); the four dense graphs place over
70\% of the vocabulary within two edges of a typical gene, fusion and co-occurrence under 2\%.
Source: \texttt{experiments/010-kuzmin-tmi/scripts/graph\_statistics.py}.}{fig:graph-priors-2}
\end{figure*}

\input{sections/tab-graphs}
\input{sections/tab-string-versions}
