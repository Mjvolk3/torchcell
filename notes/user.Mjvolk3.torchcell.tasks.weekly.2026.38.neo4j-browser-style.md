---
id: ujft0i00i2jtkckvb7bgfq7
title: neo4j-browser-style
desc: ''
updated: 1789709721689
created: 1789709721689
---

## 2026.09.18

- [x] Neo4j Browser stylesheet so `NamedThing` no longer paints every node: `database/conf/torchcell.grass` generated by [[torchcell.database.browser_style]] from the schema config and the paper's ontology lane colors; ancestor labels first, class labels last; 6 tests.
- [x] Styling applied without any upload: `database/browser/torchcell-seed.js` (generated beside the stylesheet) plants the Browser's persisted `graphStyling` slice in local storage; `database/browser/patch_browser_jar.py` puts it in the Browser jar inside `database/docker/Dockerfile.tc-neo4j-browser` ([[database.docker.Dockerfile.tc-neo4j-browser]]); served on `michaelvolk/tc-neo4j:5.26.28-browser.1` since 03:03 CDT. `torchcell` is now the default database of the served store (no more `:use torchcell`), and the live rebuild sets and asserts both.
