%% notes-tex/microbe-perturb-seq/main.tex
%% [[microbe-perturb-seq.method-review-and-costing]]
%% https://github.com/Mjvolk3/torchcell/tree/main/notes-tex/microbe-perturb-seq/main.tex
%%
%% Typeset counterpart of the dendron hierarchy notes/microbe-perturb-seq.*.md.
%% Pairing is by NAME: notes-tex/<slug>/ <-> notes/<slug>.*.md, the same rule that
%% pairs experiments/.../foo.py with notes/experiments....foo.md.
%%
%% Build:  make          -> main.pdf        (draft: status chips + provenance flags)
%%         make clean-view -> main-clean.pdf  (share view)
%%         make check    -> formatting / provenance / citation gate
%%         make bib      -> re-export references.bib from Zotero
%%
%% EVERY number in this document comes from a committed script under
%% experiments/019-perturb-seq-costing/ and is marked with a `%% SOURCE:` comment.
%% Anything not backed by the torchcell-library mirror carries a visible
%% \external{} or \secondhand{} flag that survives into the share view.

\documentclass[10pt,a4paper]{article}
\usepackage[draft]{tcdoc}

\title{\vspace{-1.5em}Multiplexed CRISPRi Perturb-seq in Yeast\\
  {\large Methods, Theoretical Limits, and Genome-Scale Cost}}
\author{Michael Volk}
\date{\today}

\begin{document}
\maketitle
\thispagestyle{empty}

\begin{abstract}
\noindent
Pooled genetic screens read out by single-cell RNA sequencing map perturbations
to their transcriptional consequences at dense, transcriptome-wide resolution.
This work establishes what such a screen costs in \org{S.\ cerevisiae}, what sets
the ceiling on how many genes can be perturbed in one cell, and which parameter
is binding. It then asks how the design scales along its two real axes,
perturbations per cell and environments per experiment, and what changes when the
host is \org{P.\ kudriavzevii} rather than \org{S.\ cerevisiae}. It serves two
purposes: selecting a method, and explaining the chemistry and the statistics
well enough that the relevant passages can be reused in a methods write-up.
Numbers are regenerated from committed scripts rather than transcribed, and every
claim not backed by a hash-pinned artifact in the library mirror carries a
visible flag, listed in the key below.
\end{abstract}

\vspace{0.5em}
{\small\color{tcgray}
\textbf{How to read this.} Almost every subsection ends in a boxed statement of what it established. Reading only those boxes, in order, gives the whole argument and every number a decision depends on; the surrounding text is the evidence for them. The exceptions are the ranked list in \cref{sec:ranked}, the plan in \cref{sec:staging} and the ordering in \cref{sec:scaling-order}, which are already summaries and should be read whole.\par\smallskip

Working notes: \file{notes/microbe-perturb-seq.*.md}\par
Generating scripts: \file{experiments/019-perturb-seq-costing/}\par
References: Zotero \file{torchcell/microbe-perturb-seq} (group $\cup$ personal)\par}

\vspace{1em}
\tccontents

\clearpage
\input{sections/1-introduction}
\input{sections/2-what-perturb-seq-measures}
\input{sections/3-method-explainers}
\input{sections/4-theoretical-limits}
\input{sections/5-application}
\input{sections/6-bottlenecks}
\input{sections/7-scaling}

\clearpage
\small
\bibliography{references}

%% --- Appendix, after the references on purpose -------------------------------
%% Provenance tables are reference material, not argument. A reader checking
%% whether "up to 96 per chip" is really what the source says needs this; every
%% other reader needs it out of the way. After the bibliography is where a
%% document conventionally puts material of exactly that kind, and every figure
%% caption still cross-references it by number.
%%
%% It is NOT gated on the draft build. The share view (`make clean-view`) keeps
%% \external and \secondhand flags for the same reason it keeps this: a
%% document whose numbers are checkable only in-house is asking to be trusted
%% rather than read, and that is the opposite of what the table is for.
\clearpage
\normalsize
%% Carries a status chip like every other heading, and it is left at `todo`
%% rather than promoted: the table is generated, but nobody has read it end to
%% end against the sources since the last two figures were added.
\section*{Appendix: Provenance of Hand-Drawn Figure Numbers\secstatus{todo}}
\addcontentsline{toc}{section}{Appendix: Provenance of Hand-Drawn Figure Numbers}
\small
\input{tables/t15-figure-provenance}

\end{document}
