# notes-tex/010-additive-baselines/Makefile
# [[experiments.010-kuzmin-tmi.additive-baseline-analysis]]
# https://github.com/Mjvolk3/torchcell/tree/main/notes-tex/010-additive-baselines/Makefile
#
# No Zotero collection yet: this document cites nothing through natbib. The
# outside works it refers to (the Visani preprint, Tran et al. 2026, Kuzmin 2018)
# are named in prose with their DOIs. The Visani and Tran items are in the
# personal library's intake collection (torchcell / torchcell-queue /
# torchcell-in); once they sit in a collection of this document's own, set
# ZOTERO_PERSONAL_COLLECTION to that collection's key, run
# `python scripts/lit_bib_store.py --name 010-additive-baselines`, then
# `make bib-pull`, and the prose mentions can become \citep.
#
# Panels come from two experiments' image directories: the 010 baselines and the
# 025 rerun of them on the replacement build, so PLOT_SLUGS lists both.

PYTHON ?= /home/michaelvolk/miniconda3/envs/torchcell/bin/python
PLOT_SLUGS := 010-kuzmin-tmi 025-solid-growth

include ../common/Makefile.common
