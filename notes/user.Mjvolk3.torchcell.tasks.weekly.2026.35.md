---
id: tqzn9662f7ym3zsbuuj8jk3
title: '35'
desc: ''
updated: 1787586795736
created: 1787586795736
---

## 2026.08.24

- [x] **Build list for the first trigenic round is final: 45 strains, 25 doubles + 20 triples, 65 on plate.** Bench sheet [[experiments.W019-echo-crispr-array.build-list]], PDF `notes/assets/pdf-output/experiments.W019-echo-crispr-array.build-list.pdf`, rationale [[experiments.W019-echo-crispr-array.next-strains-to-construct]]. Every strain builds in parallel; T01-T15 have one parent route each so start those first
- [x] Chose `capped` over five alternatives because it is the only design that holds exposure to the two zero-trigenic-data genes (YER079W, YLR312C-B) to a set level, 6 of 20, while still covering all eleven panel genes; it costs 45 strains against 40 for `rank` [[experiments.W019-echo-crispr-array.scripts.triple_design_rank_sampling]]
- [x] Corrected the target basis from 10 genes / 31 targets to 11 / 39: YLR104W (LCL2) is both built and a panel-12 prediction node, and the frozen TEN set predated run 4's decision to keep LCL2 [[experiments.W019-echo-crispr-array.scripts.triple_design_rank_sampling]]
- [x] Audited the whole round with an independent checker that re-derives inventory, basis and selection from the pinned inputs without importing the design script: **52 checks, 0 fail**. The load-bearing one is tau-computability, since a triple yields nothing unless all three of its doubles and all three of its singles are on the same plate [[experiments.W019-echo-crispr-array.scripts.verify_triple_build_list]]
- [x] Found and fixed a real error in both build notes: 17 new doubles are still needed if YER079W and YLR312C-B are dropped, not 18, and the clean core is 31 strains rather than the 32 stated or the 34 also claimed. `YLR104W + YPL081W` serves only rank 3 and is orphaned by the drop [[experiments.W019-echo-crispr-array.build-list]]
- [x] Fixed the Kuzmin replicate-reproducibility numbers in the run-4 handoff to the SI text rather than a figure read: digenic eps and raw trigenic r = 0.90-0.91, adjusted trigenic tau r = 0.74-0.81, sha256-pinned. The old 0.59 for adjusted tau understated their reproducibility by 0.15-0.22 [[experiments.W019-echo-crispr-array.run4-handoff]]
- [x] Labeled the per-gene digenic column as not training data; it comes from `dmi_kuzmin{2018,2020}`, which `001_small_build.cql` excludes, and the trigenic counts were read from the LMDBs rather than quoted [[experiments.W019-echo-crispr-array.next-strains-to-construct]]
- [x] Deprecated the 10-gene / 31-target artifacts, 19 files, to `/scratch/projects/torchcell-deprecated/2026-08-24_104110__*/` with per-file provenance manifests [[experiments.W019-echo-crispr-array.session-handoff-2026-08-23]]
- [x] Added what run 4 measured for every existing strain to the build sheet, and a gap table with a reason for each of the four things missing: **12 of 12 singles and 13 of 13 built doubles were measured**, but YLR104W (LCL2) has no published SMF (the reference panel was built for the earlier design that carried LCL1) and d2 `YPL046C + YPL081W` has no published DMF (tier `novel`, nobody has made it). The 14th designed double failed with **no recorded date or cause**, and no triple has ever been constructed [[experiments.W019-echo-crispr-array.scripts.run4_measured_summary]]
- [x] Kept epsilon out of that table on purpose. Run 4's eps is not reportable, the double/single ratio is 0.758 where a multiplicative model wants ~1.07, so the sheet carries fitness and names the problem rather than showing a number that would be read as a result [[experiments.W019-echo-crispr-array.run4-handoff]]
- [x] **Perturb-seq method review published as Zotero v14 then v15** (`notes-tex/microbe-perturb-seq`). Added Sec. 7 on scaling, rewrote rRNA depletion, added the UIUC core meeting and Ultima, and four figures. Landed through the merge queue [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [x] Corrected the rRNA section on three counts: depletion acts AFTER fragmentation and indexing, M3-seq tested Cas9 against RNase H and chose RNase H, and there are four chemistries rather than one. The earlier text cited M3-seq as corroborating the Cas9 route when it argues against it [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [x] Found the 5' assay captures guides with a scaffold primer rather than an engineered capture sequence, so an unmodified Pol III library may already be readable. If it holds, the guide-capture construction project collapses into a one-channel pilot [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [x] Fixed a Sec. 4.1 incoherence: 137/359/699 paired the two-fold coefficient A=16.3 with phi=1.1, a phi calibrated to the measured 1.34-fold A=90.9, implying an 18-cell floor in a section relying on 100 [[experiments.024-perturb-seq-costing.scripts.derived_values]]
- [x] `check_doc.py` now fails on an oversized float. Fig. 10's caption was overflowing by 88.7pt and LaTeX was DROPPING the projection caveat, which the gate did not see because it only matched overfull hbox/vbox [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [x] Addressed all eight review comments on v14, including a k-labelling fix across tables 7, 10 and 18, and a new library-ceiling figure [[experiments.024-perturb-seq-costing.scripts.plot_library_ceiling]]
- [x] Added a Poisson primer placed before the arithmetic that needs it, sharing `lam_for_target_mean` with the table so the two cannot drift [[experiments.024-perturb-seq-costing.scripts.plot_poisson_primer]]
- [x] Restored the primary checkout to `main`. It was on `paper/figures-fig1`, 398 commits behind, which is why the `deprecate`, `drawio-diagram` and `lit-pull` skills were invisible. That branch is now a proper worktree and is pushed, since origin had lost it and its 11 commits existed only on this Mac
- [x] Gitignored `dev/merge_queue/`. The queue stores state at `$DATA_ROOT/dev/merge_queue/`, which is outside the repo on GilaHyper but inside it on this Mac, so it read as permanent untracked clutter and made every published PDF stamp `-dirty`
- [x] Graduated four scratch notes and retired a fifth as a build intermediate. Two were superseded, one was a precursor worth keeping [[experiments.024-perturb-seq-costing.spc-capseq-design-sketch]] [[experiments.W019-echo-crispr-array.run2-handoffs]]
- [x] Recovered three scratch notes that vanished from disk out of dangling git objects, after `sudo` blocked the APFS snapshot route. They were once staged, so their blobs survived [[experiments.W019-echo-crispr-array.run2-handoffs]]
- [x] **Addressed all 45 review comments on Perturb-seq v14** and renumbered the experiment `019-perturb-seq-costing` to `024`, since `019` collided with two unrelated experiments and shared nothing with either. Full ledger in `notes-tex/microbe-perturb-seq/review/round-2-dispositions.md` [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [x] Every one of the 45 comments fell on pages 3 to 6, the introduction plus the controlled vocabulary, and none on the argument sections. Round 1 rebuilt the glossary for lookup; round 2 says a lookup entry still has to explain, and about half the comments are an entry that named a thing without saying why it is that way [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [x] **"The first demonstration in any microbe" does not survive verification.** Brandner et al. scope their own claim to bacteria (`paper.md:39`), and two microbial pooled screens read out by scRNA-seq predate it, Jackson 2020 and Nadal-Ribelles 2025, the second cited in the same sentence. It contradicted itself. Now "the first single-cell CRISPR screen in that domain" [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [x] Fixed a real error in the glossary: template switching is not always bead-borne. It is on the bead in droplet 5' chemistry and added in bulk after lysis in split-pool, where it carries no barcode because the barcode was written three rounds earlier [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [x] Renamed Yao et al.'s sparsity `q` to `nu`, retiring a paragraph headed "A warning about the letter q". A warning that has to be repeated at every use is a naming decision that was not made. Same for "index", which now means the Illumina index read and nothing else [[writing-style-guide]]
- [x] **American spelling now checked as a class rather than a word list.** `check_doc.py` held `permeabilised` but not `permeabilises`, `optimise` but not `factorise`; it is now stem-plus-endings for `-ise`/`-yse` plus explicit words for `-our`, `-re` and doubled-l. Caught 23 live violations with no false positives, including one in the style guide's own text [[writing-style-guide]]
- [x] Glossary entries that assert a number now carry their citation into the table. The `citation_key` field existed on `Term` and was never emitted; one `rendered_definition()` helper covers every present and future entry [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [x] Moved the Poisson primer from the end of Sec. 7.1 to a new Sec. 4.1 that introduces the distribution once and names all three appearances, shot noise, droplet loading and plasmid uptake. It had been the last figure in the document while the first thing it explains happens thirty pages earlier [[experiments.024-perturb-seq-costing.scripts.plot_poisson_primer]]
- [x] Rebuilt that figure to the house bar style. It was the one plot in the folder filling bar faces with the pale `PLOT_PALETTE_FILL` companions and drawing borders in the line color, which is backwards. Widening from `wide` to `full` is what fixed the cropped (c) panel letter and the panel (b) legend sitting on its own bars [[experiments.024-perturb-seq-costing.scripts.plot_poisson_primer]]
- [x] Answered the freeze-thaw objection to the plate amortization with `plate_set_lifetime()`: one plate set is 103M cells, about 16 genome-scale screens, and freeze-thaw resolves by aliquoting rather than a shorter plate life, since `w` working plates each see `215/w` cycles. Flagged that the tolerance itself is not in the mirror [[experiments.024-perturb-seq-costing.scripts.derived_values]]
- [x] Corrected the host section. The genetics are not *published* for *P. kudriavzevii*, which is not the same as absent: a working CRISPRi system and a JGI assembly are held in house and now carry `\external{in-house, unpublished}`. The reason the first experiment carries no perturbations is restated as footing rather than absence [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [x] **Added Fig 7, linker ligation against template switching**, after a question on Fig 2 about the difference. Yellow marks sequence written by a polymerase during the step drawn, so panel (a) has none and the contrast is visual rather than asserted in the caption [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [x] **The glossary said split-pool has no TSO, and that is wrong.** `Splint` ended "Split-pool uses splints; droplet 5' chemistry uses template switching", which fails twice: split-pool template-switches for its second handle, and scifi-RNA-seq is a droplet method whose barcode arrives by ligation [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [x] **3' libraries template-switch too.** It is not a 5' feature; it is how the second PCR handle gets on, and a molecule handled at one end cannot be amplified. 3' vs 5' names which oligo carries the barcode: 3' puts it on the oligo-dT with the TSO free, 5' swaps them, and that swap is why the 5' kit might read a Pol III guide [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [x] Corrected two things I wrote the day before. The Brettner TSO step is **bead-bound**, not bulk (`md:161`, section "Bead-bound template switch reaction"), and it is not a pure template switch but a second RT pass the protocol needs anyway because formaldehyde crosslinking hobbled the in-cell reaction, with the TSO riding along (`md:53`) [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [x] Placed the new figure at the end of Sec. 3.7 rather than beside Fig 2, because the draw.io canvases of Figs 1-6 hardcode cross-references to each other and a figure inserted earlier falsifies text baked into exported PDFs. It lands as Fig 7 and Figs 1-6 are untouched [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [x] Fig 5: H was defined only in the color key at the foot, three uses after it first appears. Title now says "the handle H" and the caption defines H and CB up front. Fig 3: tightened the "What this avoids" box, which had a large gap above its title and was taller than its content [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [ ] **Land Fig 7 and Fig 8.** `paper/figures-fig1` has 11 unlanded commits and rebasing stops on an add/add conflict in `torchcell/sga/`, where main's version is newer and larger. Commit `48396713` is the July SGA strand and looks superseded; skipping it rather than merging is a judgement call #high
- [ ] Decide whether the RNase H probe route or a Cas9 pool is tried first for rRNA depletion in yeast. Untested either way; the probe route is a design problem with a known answer where the Cas9 pool needs redesign and re-titration against yeast coverage #medium [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [ ] Test 5' guide capture on the existing S. cerevisiae library, in parallel with the unperturbed P. kudriavzevii run rather than after it. One channel, and it decides whether anything has to be built #high [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [ ] Confirm three core numbers in writing before any budget quotes them: the 3.82 vs 3.2 billion per lane, the $153 test counts against a page that says free, and the Ultima per-lane rate that cannot be both $1,990 and $3,310 for three #medium
- [ ] Work out what removed three untracked scratch notes from disk today. Recovered, but the cause is unexplained and the same thing could take an unrecoverable file #medium
- [ ] Record an attempt date and outcome if `YKL033W-A x YJR060W` is retried. Hypothesis (untested) for the original failure: YJR060W (CBF1) is the sickest single in the panel at 0.5348 against 0.8390 for the next lowest, but nothing was recorded and nothing has been tested #low [[experiments.W019-echo-crispr-array.build-list]]
- [ ] **Settle YLR312C-B in or out before starting the starred strains** (D07, D10, D11, D13, D19, D22, D23 and T01-T06). SGD "ORF, Merged", no discrete protein, zero trigenic training data, 0 of its 9 in-panel digenics significant; the swap to SPH1 was recommended and never actioned #high [[experiments.010-kuzmin-tmi.inference-dataset-3]]
- [ ] **tau is not callable at current replication.** SE(tau) ~ sqrt(7)*s and at 2 picks / 3 plates s ~ 0.121, so the smallest callable |tau| is 0.63 against Kuzmin's 0.08 / 0.12 / 0.20 thresholds and their median SE(tau) of 0.031. 0 of 39 targets clear our floor; closing it is a pick/plate problem, not a model problem #high [[experiments.W019-echo-crispr-array.next-strains-to-construct]]
- [ ] Decide the round order: the pick-replication round (26 strains / 57 tubes) and this triple round (65 strains / 65 tubes) compete for the same plate. Recommended is replication first, constructing these 45 during it #high [[experiments.W019-echo-crispr-array.build-list]]
- [ ] Check the 0.43 calibration slope against the inference parquet; it assumes mu_pred ~ mu_y and one read would settle it #medium [[experiments.W019-echo-crispr-array.next-strains-to-construct]]
- [ ] Record which variance decomposition gives the 6-plate floor of 0.55; scaling s by 1/sqrt(2) from 3 to 6 plates gives 0.44, and a nested model where only the plate term shrinks is the more careful assumption but is not shown #low [[experiments.W019-echo-crispr-array.session-handoff-2026-08-23]]
- [ ] If anyone retries `YKL033W-A x YJR060W`, record the attempt date and outcome; the original failure has no recorded date, and it blocks 0 of the 39 targets so it stays off the list until then #low [[experiments.W019-echo-crispr-array.build-list]]

## 2026.08.25

- [x] **Cleaned up the 12 untracked scratch notes on main: 6 graduated, 6 retired to the graveyard.** The split is whether the note holds a design or a solved incident; every Delta launch runbook and the Claude-permissions paste block are incidents, the decoder taxonomy and the perturbation-operator audit were already graduated verbatim into tracked notes
- [x] The metabolism explainer is now a real note. Its conclusions were already consolidated, but the derivations from definitions up, Parts 0 to 7 plus the Q1 to Q15 read-through, existed only in an untracked file that three tracked notes called authoritative [[plan.cgt-metabolism-flux-layer.explainer]]
- [x] Kept the graph-prior probe because it is unbuilt and it gates the graph channel: nothing in the repo has ever tested whether proximity on the nine adjacency matrices predicts which reporters respond to a deletion, which is the assumption the KL regularizer encodes [[experiments.019-simb-multimodal.graph-prior-probe]]
- [x] Kept wave 6 whole rather than folding it into the arm notes. Components A to C landed elsewhere, but the generalization bound, train Pearson 0.72 against a 0.775 ceiling is evidence of memorization rather than a wall, and the per-epoch cost decomposition are recorded nowhere else [[experiments.019-simb-multimodal.wave6-design]]
- [x] Folded the energy-score derivation into the loss note, which carried the implementation and none of the why, and the Delta metabolism design, the saturation finding and the 24-run grid, into the metabolism plan, whose record stopped at 2026.07.28 [[torchcell.losses.distributional]] [[plan.metabolism-003-optimizer-distributional.2026.07.27]]
- [x] Repointed six inbound references that pointed at now-retired scratch notes, and fixed a broken dendron link in the Fig-3 note that had swallowed a bullet into a link to a deprecated file [[plan.cgt-metabolism.2026.07.25]] [[experiments.019-simb-multimodal.fig3-expression-experiments]]
- [x] The Zotero capture gaps are an open backlog, not an incident: 24 items that `lit_sync.py` cannot reach because its capture path is keyed on DOI plus PDF attachment [[torchcell.literature.sync.zotero-capture-gaps]]
- [x] **Round 1 on the W019 bench sheet: all eight comments, revision back in Zotero.** Ledger in `notes-tex/w019-strain-build-list/review/round-1-dispositions.md` [[experiments.W019-echo-crispr-array.build-list]]
- [x] **The usable panel is eleven, not twelve.** The design panel holds YIL174W, which was never built, and not SPH1, which was; setting YLR312C-B aside leaves ten, and it sits in six of the twenty triples. SPH1 is in zero of them, counted from the selection file rather than asserted [[experiments.W019-echo-crispr-array.build-list]]
- [x] **Named the published statistics in the tables, not only in the prose.** Both SE columns now read `boot SE` with the resampling unit under them, 3 plates against 17 screens, because they are the same statistic; the doubles column is `colony SD`, a spread over the colonies of one screen that compares to our plate SD. The 17 traces from the `_sn` NatMX query-strain ids to the SI quote [[torchcell.datasets.scerevisiae.costanzo2016.noise-computation]]
- [x] **Published version 2 with two comments only half addressed**, both answered somewhere other than where the comment pointed: [4] in the caption while the header kept the wrong label, [6] cut from one sentence and left in a generated caption. Fixed in version 3, and a review round is now closed by grepping the rendered PDF per comment before publishing, not by rereading the diff [[experiments.W019-echo-crispr-array.build-list]]
- [x] The provenance key now prints only in a document that uses a flag, detected from the `.aux`. It needs `\global`: `\newif` assigns locally and the `.aux` is read inside a group, so the first version silently dropped the key from microbe-perturb-seq [[experiments.W019-echo-crispr-array.build-list]]
- [x] **Handoff bundle for the bench: one zip with the share-view PDF, the five table CSVs and a sha256 manifest**, at `/tmp/screenshots/W019-echo-crispr-array/w019-strain-build-list.zip`. Deterministic, so an unchanged rebuild gives the same hash; the zip itself is gitignored because every byte in it is already tracked [[experiments.W019-echo-crispr-array.scripts.build_list_bundle]]
- [x] **Perturb-seq figure round 3: Fig 7 redrawn and promoted to Fig 3, and the renumbering carried through five draw.io canvases.** The tex was free because every reference is a symbolic cref; the cost was twelve figure numbers baked into exported canvas text, verified by reading them back out with pdftotext. Two were already stale before the move [[microbe-perturb-seq.method-review-and-costing]]
- [x] Fig 3 (b) now draws the poly(A) tail with the oligo-dT annealed to it, which is what explains why the non-templated Cs land at the far end, and **corrects the TSO orientation**: it was drawn parallel to the strand it templates, which is not a structure that can exist [[microbe-perturb-seq.method-review-and-costing]]
- [x] Fig 3 (a) carries strand count in brackets rather than in the caption, so ligation is visible as two-brackets-become-one; a row of abutting blocks reads as one molecule however the caption is worded [[microbe-perturb-seq.method-review-and-costing]]
- [x] Two new one-row three-panel scaling figures: Fig 16 for Sec 7.2 (plex is cheap for main effects, named pairs are still absent, panel size is the lever) and Fig 17 for Sec 7.3 (environments are linear, the round-1 plate is idle, droplet pays per condition) [[experiments.024-perturb-seq-costing.scripts.plot_recovery]] [[experiments.024-perturb-seq-costing.scripts.plot_environments]]
- [x] Fig 17c gained preindexed droplet as a third series after review. Drawing only un-preindexed 10x overstated the environment penalty by the factor preindexing removes, and Sec 5.7 already promotes preindexed droplet to co-equal [[microbe-perturb-seq.method-review-and-costing]]
- [x] Fig 12c hatch replaced by two shades plus a legend, Fig 13c batch-unit labels moved above their rules, Fig 15f right axis moved off blue to purple, and five keyboxes gained the symbol glosses they were missing [[microbe-perturb-seq.method-review-and-costing]]
- [x] **The 008 FFA epistasis p-values were referred to df 1 to 2 instead of the Welch-Satterthwaite effective df of the propagated combination, which suppressed nearly everything: FDR-significant went from 0 of 990 to 456 of 990 once corrected.** "No positive trigenic interaction survives FDR" was an artifact of the reference distribution, not a property of the data [[plan.008-xue-ffa-epistasis-audit.2026.08.15]]
- [x] Swept every t and z significance test in `experiments/` and `torchcell/` for the same pattern: 008 was the only affected site, and the four other hand-supplied df are each correct for the quantity they test [[plan.008-xue-ffa-epistasis-audit.2026.08.15]]
- [x] Removed the `df=2` default from `compute_se_pvalue` so a caller who omits it fails loudly rather than silently reproducing the bug; both models reproduce the landed numbers exactly [[experiments.008-xue-ffa.scripts.free_fatty_acid_interactions]] [[experiments.008-xue-ffa.scripts.additive_free_fatty_acid_interactions]]
- [x] Positive three-way epistasis on FFA titer is linear-scale only: 73 trigenic interactions are FDR-significant in all four models with zero sign discordance, but that consensus is entirely negative and the two log-scale models find no positive interaction at all [[experiments.008-xue-ffa.scripts.ffa_epistatic_path_panels]]
- [x] **Perturb-seq figure round 3: Fig 7 redrawn and promoted to Fig 3, and the renumbering carried through five draw.io canvases.** The tex was free because every reference is a symbolic cref; the cost was twelve figure numbers baked into exported canvas text, verified by reading them back out with pdftotext. Two were already stale before the move [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [x] Fig 3 (b) now draws the poly(A) tail with the oligo-dT annealed to it, which is what explains why the non-templated Cs land at the far end, and **corrects the TSO orientation**: it was drawn parallel to the strand it templates, which is not a structure that can exist [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [x] Fig 3 (a) carries strand count in brackets rather than in the caption, so ligation is visible as two-brackets-become-one; a row of abutting blocks reads as one molecule however the caption is worded [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [x] Two new one-row three-panel scaling figures: Fig 16 for Sec 7.2 (plex is cheap for main effects, named pairs are still absent, panel size is the lever) and Fig 17 for Sec 7.3 (environments are linear, the round-1 plate is idle, droplet pays per condition) [[experiments.024-perturb-seq-costing.scripts.plot_recovery]] [[experiments.024-perturb-seq-costing.scripts.plot_environments]]
- [x] Fig 17c gained preindexed droplet as a third series after review. Drawing only un-preindexed 10x overstated the environment penalty by the factor preindexing removes, and Sec 5.7 already promotes preindexed droplet to co-equal [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [x] Fig 12c hatch replaced by two shades plus a legend, Fig 13c batch-unit labels moved above their rules, Fig 15f right axis moved off blue to purple, and five keyboxes gained the symbol glosses they were missing [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [x] **Renamed the perturb-seq document to the 024 namespace so the Zotero collection is the experiment number.** `notes-tex/024-perturb-seq-costing/`, four Dendron notes moved to `experiments.024-perturb-seq-costing.*` with all 34 wikilinks repointed by `dendron-cli note move`, five tracked assets renamed to match [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [x] Kept the 18 published versions attached by renaming the Zotero collection in place rather than letting a new one be created, and repointing the parent item's `Doc Key:` marker. Renaming only the collection was not enough: the next publish would have made a second parent report inside the right collection, which `--dry-run` caught [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [x] Left the literature collection `torchcell-topics/microbe-perturb-seq` alone. Two collections share that name and only the notes-tex one is ours to rename; the other feeds `make bib` and the lit sync [[torchcell.literature.sync]]
- [x] Baked the Dendron id into `render_tables.py`'s frontmatter template. It writes that note whole, so the next regeneration would have stripped the id and made the note unmovable again [[experiments.024-perturb-seq-costing.scripts.derived_values]]
- [x] Dropped 18 duplicate table files the 024 rename left tracked under the dead `notes-tex/microbe-perturb-seq/` path. Verified byte-identical against the new path before removing; cause of the partial `git mv` not established and not guessed at. `make check` was green on the branch that introduced it, so a green gate does not rule out this kind of drift [[experiments.024-perturb-seq-costing.method-review-and-costing]]
- [x] **The manuscript now publishes to Zotero for review like the notes-tex documents do.** `make -C paper/nature-biotech publish` puts `editing.pdf` under `torchcell / paper / nature-biotech`, hash-versioned so a comment always names the build it was made on [[notes-tex.common.zotero_publish]]
- [x] Kept the collection path derived from the repo directory rather than configured, so `paper/nature-biotech/` gives `torchcell/paper/nature-biotech` and cannot drift. The `paper` parent already existed and was empty; only the leaf was created [[notes-tex.common.zotero_publish]]
- [x] Verified backward compatibility before publishing anything: the perturb-seq document's 19 versions are found by a `Doc Key:` that is the repo-relative directory, so a bare name still resolves to the same collection `AKUUM8DH` and parent `IW46KK3N` with an unchanged filename [[notes-tex.common.zotero_publish]]
- [x] Fixed an author bug the dry run caught: the Zotero creator was built from the first and last word of the joined author string, which on three authors gives "Michael Zhao". Now one creator per `\fnm`/`\sur` pair [[notes-tex.common.zotero_publish]]
- [x] **Tested whether the log scale explains why the two log-scale 008 models find no positive trigenic interaction: it explains 15 of 27, and power explains the other 12.** The transform is asymmetric, 56 percent of linear-positive triples go non-positive on the log scale and no negative ever becomes positive, so a positive interaction on FFA titer is a statement about the scale it was measured on [[experiments.008-xue-ffa.scripts.linear_vs_log_scale_sign_test]]
- [x] The GLM log-link and log-OLS visualization scripts had never produced a figure: they load a pickle no model script wrote, print "Results file not found" and exit 0. The log-scale models now have 14 and 13 interaction figures against 0 before [[experiments.008-xue-ffa.scripts.linear_vs_log_scale_sign_test]]
- [x] **The paper build was not reproducible, so Zotero hash-dedupe never fired.** `SOURCE_DATE_EPOCH` lives in `notes-tex/common/Makefile.common` and the paper has its own Makefile that never set it; two rebuilds of untouched sources gave different sha256 and every `make publish` would pile up a near-identical PDF. Three forced rebuilds now agree [[notes-tex.common.zotero_publish]]
